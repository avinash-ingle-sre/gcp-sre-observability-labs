apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: prometheus-test
  namespace: gmp-aijl
  labels:
    app.kubernetes.io/name: prometheus-test
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: prometheus-test
  endpoints:
  - port: metrics
    interval: 60s
